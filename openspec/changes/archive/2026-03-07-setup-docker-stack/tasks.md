## 1. Docker Compose Stack

- [x] 1.1 Create `docker-compose.yml` with MariaDB, EspoCRM, and EspoCRM daemon services
- [x] 1.2 Configure volumes for database persistence and EspoCRM data
- [x] 1.3 Add `.env` file (or `.env.example`) with default credentials
- [x] 1.4 Verify stack starts cleanly with `docker compose up -d`
- [x] 1.5 Verify EspoCRM UI is accessible and admin login works
- [x] 1.6 Verify EspoCRM REST API responds (e.g. `GET /api/v1/App/user`)
