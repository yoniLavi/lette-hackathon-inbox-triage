[project]
name = "crm-cli"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "click>=8.0",
    "httpx>=0.27",
]

[project.scripts]
crm = "crm_cli.main:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
